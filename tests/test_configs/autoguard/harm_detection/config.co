define subflow check input harm detection
  $result = execute call autoguard harm detection api
  if $result == True
     # We need to provide a warning in this case
     bot refuse to respond harm detection
     stop

def bot refuse to respond harm detection
  "Harm to human in prompt detected. I'm sorry, I can't respond to that."

define subflow check output harm detection
  $result = execute call autoguard harm detection api
  if $result == True
     # We need to provide a warning in this case
     $bot_message = $bot_message + " Attention: Response has potential harm to human"
