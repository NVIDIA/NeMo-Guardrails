models:
  - type: main
    engine: openai
    model: gpt-3.5-turbo
    parameters:
      temperature: 0.0
rails:
  input:
    flows:
      - check input harm detection
  output:
    flows:
      - check output harm detection
