models:
  - type: main
    engine: openai
    model: gpt-3.5-turbo
    parameters:
      temperature: 0.0
rails:
  input:
    flows:
      - check input confidential detection
  output:
    flows:
      - check output confidential detection
