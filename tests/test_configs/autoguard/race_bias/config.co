define subflow check input race bias
  $result = execute call autoguard race bias api
  if $result == True
     # We need to provide a warning in this case
     bot refuse to respond race bias
     stop

def bot refuse to respond race bias
  "Race bias in prompt detected. I'm sorry, I can't respond to that."

define subflow check output race bias
  $result = execute call autoguard race bias api
  if $result == True
     # We need to provide a warning in this case
     $bot_message = $bot_message + " Attention: Response has potential race bias"
