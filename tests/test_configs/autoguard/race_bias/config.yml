models:
  - type: main
    engine: openai
    model: gpt-3.5-turbo
    parameters:
      temperature: 0.0
rails:
  input:
    flows:
      - check input race bias
  output:
    flows:
      - check output race bias
