models:
  - type: main
    engine: nemollm
    model: gpt-43b-002

custom_data:
  fact_checking:
    provider: align_score  # or ask_llm
    parameters:
      endpoint: "http://localhost:5000/alignscore_large"
