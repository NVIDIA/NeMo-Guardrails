models:
  - type: main
    engine: nemollm
    model: gpt-43b-002

rails:
  config:
    autoguard:
      parameters:
        autoguard_topical_endpoint: "http://35.225.99.81:8888/topical_detection"
  output:
    flows:
      - call autoguard topical
