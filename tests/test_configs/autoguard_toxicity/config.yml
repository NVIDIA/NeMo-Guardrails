models:
  - type: main
    engine: nemollm
    model: gpt-43b-002

rails:
  config:
    autoguard:
      parameters:
        toxicity_endpoint: "http://35.225.99.81:8888/guardrail"
      matching_rules:
        { "text_toxicity_extraction": { "score": 0.5 } }
  input:
    flows:
      - autoguard toxicity input
