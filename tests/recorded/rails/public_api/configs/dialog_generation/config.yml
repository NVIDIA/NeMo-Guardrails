models:
  - type: main
    engine: openai
    model: gpt-5.4-nano
    parameters:
      max_retries: 0
core:
  embedding_search_provider:
    name: simple
knowledge_base:
  embedding_search_provider:
    name: simple
