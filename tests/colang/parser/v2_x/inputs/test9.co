flow return a name
  """Flow name starts with keyword `return`"""
  bot say "Here's a name"

flow a return flow
  """Flow name ends with `flow` keyword."""
  bot say "Here's a name"
  await return a name

flow start walking
  """A flow name that contains 'start'"""
  await animation "start walking"

flow x
  """Await on a flow whose name starts with a keyword"""
  await start walking
