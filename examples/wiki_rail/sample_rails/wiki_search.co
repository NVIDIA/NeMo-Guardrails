define bot remove last message
  "(remove last message)"

define bot inform cannot answer question
 "I cannot answer the question"

define user ask wiki search
  "Search wikipedia with keywords:"
  "Find on wiki"
  "return results from wiki"
  "Query wikipedia"

def bot answer wiki search complete
  "Complete, querying wiki search API"

define flow wiki search
  user ask wiki search
  $is_saved = execute query wiki search
  if $is_saved
    bot answer wiki search complete
