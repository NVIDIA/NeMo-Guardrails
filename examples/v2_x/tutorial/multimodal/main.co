# Example that show a simple multimodal 'hello world'
# To trigger the bot response type in `Hi`, `Hello`
# or simulate a user appearing by typing `/PresenceUserActionFinished(user_id=1)`

import avatars

flow main
    user greeted or user became present
    bot greet

flow user greeted
  user said "Hi"
    or user said "Hello"

flow bot greet
  (bot say "Hi there!"
    or bot say "Welcome!"
    or bot say "Hello!")
    and bot gesture "Smile and wave with one hand."