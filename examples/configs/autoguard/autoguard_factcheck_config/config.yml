models:
  - type: main
    engine: openai
    model: gpt-3.5-turbo
rails:
  config:
    autoguard:
      parameters:
        fact_check_endpoint: "https://nvidia.autoalign.ai/factcheck"
  output:
    flows:
      - autoguard factcheck output
